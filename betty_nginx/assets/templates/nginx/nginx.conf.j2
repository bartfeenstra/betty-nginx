{% macro headers(debug=False, https=False) %}
add_header Vary Accept-Language;
{% if https %}
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
{% endif %}
{% if debug %}
    add_header Cache-Control "no-cache";
{% else %}
    add_header Cache-Control "max-age=86400";
{% endif %}
{% endmacro %}

{% if legacy_entity_redirects %}
    map $uri $legacy_entity_redirect {
        default "none";
        {% set url_path_prefix -%}
            {% if project.multilingual -%}
                /{{ project.locales | map(attribute="alias") | join("|") }}
            {%- endif %}
        {%- endset %}
        {% for entity in project.ancestry if entity.plugin().public_facing %}
            ~^({{ url_path_prefix }})?/{{ entity.plugin().id }}/{{ entity.id }}(\/.*)*$ $1/{{ entity.plugin().id }}/{{ entity.public_id }}$2;
        {% endfor %}
    }
{% endif %}

{% if https %}
    server {
        listen 80;
        server_name {{ server_name }};
        return 301 https://$host$request_uri;
    }
{% endif %}
server {
    {{ headers(
        debug=project.debug,
        https=https
    ) }}
    {% if https %}
	    listen 443 ssl http2;
    {% else %}
	    listen 80;
    {% endif %}
	server_name {{ server_name }};
	root {{ www_directory }};
    gzip on;
    gzip_disable "msie6";
    gzip_vary on;
    gzip_types text/css application/javascript application/json application/xml;

    {% if project.clean_urls %}
        set_by_lua_block $media_type_extension {
            local available_media_types = {'text/html', 'application/json'}
            local media_type_extensions = {}
            media_type_extensions['text/html'] = 'html'
            media_type_extensions['application/json'] = 'json'
            local media_type = require('content_negotiation').negotiate(ngx.req.get_headers()['Accept'], available_media_types)
            return media_type_extensions[media_type]
        }
    {% else %}
        set $media_type_extension html;
    {% endif %}
    index index.$media_type_extension;

    {% if legacy_entity_redirects %}
        if ($legacy_entity_redirect != "none") {
            rewrite ^ $legacy_entity_redirect permanent;
        }
    {% endif %}


    {% if project.multilingual %}
        location @localized_redirect {
            {% if project.clean_urls %}
                set_by_lua_block $locale_alias {
                    local available_locales = {'{{ project.locales | map(attribute='locale') | map('to_language_tag') | join("', '") }}'}
                    local locale_aliases = {}
                    {% for project_locale in project.locales %}
                        locale_aliases['{{ project_locale.locale | to_language_tag }}'] = '{{ project_locale.alias }}'
                    {% endfor %}
                    local locale = require('content_negotiation').negotiate(ngx.req.get_headers()['Accept-Language'], available_locales)
                    return locale_aliases[locale]
                }
                {{ headers(
                    debug=project.debug,
                    https=https
                ) }}
                add_header Content-Language "$locale_alias" always;

                return 307 /$locale_alias$uri;
            {% else %}
                set $locale_alias {{ project.default_locale.alias }};
                return 301 /$locale_alias$uri;
            {% endif %}
        }

        # The front page.
        location = / {
            # nginx does not support redirecting to named locations, so we use try_files with an empty first
            # argument and assume that never matches a real file.
            try_files '' @localized_redirect;
        }

        # Localized resources.
        location ~* ^/({{ project.locales | map(attribute='alias') | join('|') }})(/|$) {
            set $locale $1;

            {{ headers(
                debug=project.debug,
                https=https
            ) }}
            add_header Content-Language "$locale" always;

            # Handle HTTP error responses.
            error_page 401 /$locale/.error/401.$media_type_extension;
            error_page 403 /$locale/.error/403.$media_type_extension;
            error_page 404 /$locale/.error/404.$media_type_extension;
            location ~ ^/$locale/\.error {
                internal;
            }

            try_files $uri $uri/ =404;
        }

        # Static resources.
        location / {
            # Handle HTTP error responses.
            error_page 401 /{{ project.default_locale.alias }}/.error/401.$media_type_extension;
            error_page 403 /{{ project.default_locale.alias }}/.error/403.$media_type_extension;
            error_page 404 /{{ project.default_locale.alias }}/.error/404.$media_type_extension;
            location ~ ^/{{ project.default_locale.alias }}/\.error {
                internal;
            }

            try_files $uri $uri/ =404;
        }
    {% else %}
        location / {
            # Handle HTTP error responses.
            error_page 401 /.error/401.$media_type_extension;
            error_page 403 /.error/403.$media_type_extension;
            error_page 404 /.error/404.$media_type_extension;
            location /.error {
                internal;
            }

            try_files $uri $uri/ =404;
        }
    {% endif %}
}
